# 🚀 VIPauto CRM - Система управления автосервисом

Полнофункциональная CRM-система для автосервиса с мобильным-first подходом, созданная на современных технологиях.

## ✨ Возможности

### 🎯 Основной функционал
- **Управление заказами** - полный цикл от создания до выдачи
- **Клиентская база** - учет клиентов, автомобилей и долгов
- **Магазин запчастей** - отдельный модуль продаж запчастей
- **Распределение заказов** - между мастерами с процентами
- **Автоматический расчет зарплат** - ежедневный подсчет в 20:00
- **Бонусы директора** - мотивация лучших сотрудников
- **Аналитика и статистика** - касса, топ-мастера, графики

### 👥 Роли пользователей
- **Директор** (Владимир Михайлович Орлов) - полный доступ + аналитика
- **Админ** (Роман) - мастер + магазин + права директора  
- **Мастера** (Владимир Чекало, Владимир Архипов, Андрей, Алексей, Артём)

### 📱 Мобильные особенности
- **PWA** - работает как нативное приложение
- **Offline-first** - функционал без интернета
- **Touch-friendly** - оптимизация для сенсорных экранов
- **Push уведомления** - важные события
- **Swipe жесты** - быстрые действия

## 🛠️ Технологический стек

### Frontend
- **React 19** - последняя версия с новыми фичами
- **Vite** - быстрый сборщик и HMR
- **TailwindCSS** - utility-first CSS фреймворк
- **TypeScript** - строгая типизация
- **React Router** - клиентский роутинг
- **React Query** - серверное состояние
- **Zustand** - клиентское состояние
- **PWA** - оффлайн функциональность

### Backend
- **Node.js 20** - последняя LTS версия
- **Express.js** - веб фреймворк
- **TypeScript** - типизация
- **Supabase** - база данных и auth
- **JWT** - токены аутентификации
- **node-cron** - планировщик задач

### База данных
- **Supabase PostgreSQL** - реляционная база
- **RLS** - Row Level Security
- **Триггеры** - автоматические расчеты
- **Миграции** - версионирование схемы

### Развертывание
- **Vercel** - frontend хостинг (бесплатно)
- **Render** - backend хостинг (бесплатно)
- **Docker** - контейнеризация
- **GitHub Actions** - CI/CD

## 🚀 Быстрый старт

### 1. Клонирование и установка
```bash
# Клонируем репозиторий
git clone https://github.com/your-username/vipauto-crm.git
cd vipauto-crm

# Устанавливаем зависимости
npm install
cd frontend && npm install
cd ../backend && npm install
cd ..
```

### 2. Настройка Supabase
```bash
# Создаем проект на https://supabase.com
# Получаем URL и ключи

# Настраиваем переменные окружения
cp .env.example .env
```

### 3. Конфигурация переменных
```bash
# .env
# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# Backend
JWT_SECRET=your-super-secret-jwt-key
NODE_ENV=development

# Twilio (опционально для SMS)
TWILIO_ACCOUNT_SID=your-twilio-sid
TWILIO_AUTH_TOKEN=your-twilio-token
TWILIO_PHONE_NUMBER=your-twilio-number
```

### 4. Настройка базы данных
```bash
# Устанавливаем Supabase CLI
npm install -g supabase

# Применяем миграции
supabase db push

# Создаем пользователей через Supabase Auth UI
# Директор: Владимир Михайлович Орлов (director)
# Админ: Роман (admin)
# Мастера: Владимир Чекало, Владимир Архипов, Андрей, Алексей, Артём (master)
```

### 5. Запуск в разработке
```bash
# Запускаем через Docker
docker-compose up -d

# Или вручную
cd backend && npm run dev &
cd frontend && npm run dev &
```

Приложение будет доступно по адресу:
- Frontend: http://localhost:3000
- Backend: http://localhost:3001

## 📱 Установка на телефон

### Android
1. Откройте http://localhost:3000 в Chrome
2. Нажмите "Добавить на главный экран"
3. Приложение появится на рабочем столе

### iOS
1. Откройте http://localhost:3000 в Safari
2. Нажмите "Поделиться" → "На экран «Домой»"
3. Приложение установится на телефон

## 🌐 Продакшн развертывание

### Автоматическое развертывание
```bash
# 1. Fork репозитория на GitHub
# 2. Создаем проекты:
#    - Vercel: https://vercel.com/new
#    - Render: https://render.com/new
#    - Supabase: https://supabase.com/new

# 3. Настраиваем переменные окружения в каждом сервисе
# 4. Пушим в main ветку - автоматический деплой!
git push origin main
```

### Ручное развертывание
```bash
# Деплой frontend
cd frontend
vercel --prod

# Деплой backend
cd backend
render deploy
```

## 📊 Структура проекта

```
vipauto-crm/
├── frontend/                    # React 19 + Vite + Tailwind
│   ├── src/
│   │   ├── components/         # UI компоненты
│   │   ├── pages/             # Страницы
│   │   ├── hooks/             # Custom hooks
│   │   ├── services/          # API сервисы
│   │   ├── store/             # Zustand store
│   │   └── types/             # TypeScript типы
│   ├── public/
│   │   ├── manifest.json      # PWA manifest
│   │   └── sw.js             # Service worker
│   └── package.json
├── backend/                     # Node.js + Express + TypeScript
│   ├── src/
│   │   ├── controllers/       # Контроллеры
│   │   ├── middleware/        # Middleware
│   │   ├── routes/           # Роуты API
│   │   ├── services/         # Бизнес-логика
│   │   └── utils/            # Утилиты
│   └── package.json
├── database/                    # Supabase миграции
│   ├── migrations/           # SQL миграции
│   └── triggers/             # Database triggers
├── docker/                      # Docker конфигурация
│   ├── docker-compose.yml
│   └── .env.example
├── .github/workflows/           # GitHub Actions
├── vercel.json                 # Vercel конфиг
├── render.yaml                 # Render конфиг
└── README.md
```

## 🔐 Аутентификация

### Поддерживаемые методы
- **Email/Password** - классический вход
- **Google OAuth** - вход через Google
- **Телефон + SMS** - вход по номеру телефона

### Роли и права доступа
```typescript
// Директор - полный доступ
const directorPermissions = [
  'orders:read', 'orders:create', 'orders:update', 'orders:delete',
  'clients:read', 'clients:create', 'clients:update', 'clients:delete',
  'parts:read', 'parts:create', 'parts:update', 'parts:delete',
  'salaries:read', 'salaries:update',
  'bonuses:read', 'bonuses:create',
  'stats:read', 'users:manage'
];

// Админ - почти полный доступ
const adminPermissions = [
  'orders:read', 'orders:create', 'orders:update',
  'clients:read', 'clients:create', 'clients:update',
  'parts:read', 'parts:create', 'parts:update',
  'salaries:read', 'stats:read'
];

// Мастер - ограниченный доступ
const masterPermissions = [
  'orders:read', 'orders:create', 'orders:update',
  'clients:read', 'clients:create',
  'parts:read', 'parts:create',
  'salaries:read:own'
];
```

## 📱 Мобильный интерфейс

### Адаптивный дизайн
- **Mobile-first** - сначала мобильная версия
- **Touch targets** - минимум 48x48px
- **Swipe gestures** - жесты для быстрых действий
- **Virtual scroll** - оптимизация больших списков

### PWA возможности
- **Offline mode** - работа без интернета
- **Push notifications** - важные события
- **App shortcuts** - быстрый доступ к функциям
- **Install prompt** - предложение установить приложение

## 🔄 API документация

### Базовый URL
- Разработка: `http://localhost:3001/api`
- Продакшн: `https://vipauto-crm-api.onrender.com/api`

### Основные эндпоинты
```typescript
// Аутентификация
POST /auth/login          // Вход
POST /auth/google         // Google OAuth
POST /auth/phone-login    // Вход по телефону
POST /auth/phone-verify   // Подтверждение SMS

// Заказы
GET  /orders              // Список заказов
POST /orders              // Создание заказа
GET  /orders/:id          // Детали заказа
PATCH /orders/:id         // Обновление заказа
PATCH /orders/:id/status  // Смена статуса

// Клиенты
GET  /clients             // Список клиентов
POST /clients             // Создание клиента
GET  /clients/:id         // Детали клиента
GET  /clients/:id/orders  // Заказы клиента

// Запчасти
GET  /parts/sales         // Продажи запчастей
POST /parts/sales         // Новая продажа

// Финансы
GET  /finance/payments    // Оплаты
POST /finance/payments    // Новая оплата
GET  /finance/debts       // Долги

// Зарплаты
GET  /salaries            // Зарплаты
PATCH /salaries/:id/pay   // Отметить оплаченной

// Статистика
GET  /stats/dashboard     // Дашборд
GET  /stats/cashflow      // Денежный поток
GET  /stats/masters       // Статистика мастеров
```

## 🗄️ База данных

### Основные таблицы
```sql
-- Пользователи (расширение Supabase Auth)
users (id, email, role, full_name, phone)

-- Клиенты
clients (id, name, phone, car1, car2, vin, notes)

-- Заказы
orders (id, client_id, services, parts_cost, services_cost, total, status)

-- Распределение заказов
order_masters (id, order_id, master_id, percent)

-- Продажи запчастей
parts_sales (id, client_name, client_phone, part_name, quantity, price, discount)

-- Долги
debts (id, client_id, order_id, amount, remaining)

-- Оплаты
payments (id, order_id, parts_sale_id, amount, type)

-- Зарплаты
salaries (id, master_id, order_id, amount, paid, week_period)

-- Бонусы
bonuses (id, director_id, order_id, amount, comment)
```

## 🧪 Тестирование

### Запуск тестов
```bash
# Frontend тесты
cd frontend
npm test

# Backend тесты
cd backend
npm test

# E2E тесты (опционально)
npm run test:e2e
```

### Тестовое покрытие
- Unit тесты - компоненты и утилиты
- Integration тесты - API эндпоинты
- E2E тесты - критичные пользовательские сценарии

## 📈 Мониторинг

### Frontend
- **Vercel Analytics** - посещения и производительность
- **React Error Boundaries** - отлов ошибок
- **Console logging** - отладка в продакшене

### Backend
- **Winston logging** - структурированные логи
- **Health checks** - мониторинг состояния
- **Error tracking** - отслеживание ошибок

### База данных
- **Supabase Logs** - логи запросов
- **Performance monitoring** - медленные запросы
- **Backup monitoring** - резервные копии

## 🔧 Разработка

### Локальная разработка
```bash
# Запуск всех сервисов
docker-compose up

# Отдельные сервисы
docker-compose up frontend
docker-compose up backend
docker-compose up postgres
```

### Код стиль
```bash
# Линтинг
npm run lint

# Форматирование
npm run format

# TypeScript проверка
npm run type-check
```

### Git workflow
```bash
# Фича ветка
git checkout -b feature/new-order-form
git commit -m "feat: add new order form"
git push origin feature/new-order-form

# Создаем PR на GitHub
# Автоматически запускаются тесты и деплой
```

## 🚨 Частые проблемы

### 1. Проблемы с аутентификацией
```bash
# Проверьте переменные окружения
echo $SUPABASE_URL
echo $SUPABASE_ANON_KEY

# Проверьте RLS политики в Supabase
supabase db diff --schema public
```

### 2. Проблемы с PWA
```bash
# Проверьте manifest.json
# Убедитесь что service worker зарегистрирован
# Проверьте HTTPS в продакшене
```

### 3. Проблемы с деплоем
```bash
# Проверьте GitHub Secrets
# Убедитесь что все переменные настроены
# Проверьте логи CI/CD
```

## 📞 Поддержка

### Документация
- [API спецификация](./api-specification.md)
- [Архитектура](./architecture.md)
- [База данных](./database-schema.md)
- [Frontend компоненты](./frontend-components.md)
- [Развертывание](./deployment-guide.md)

### Контакты
- GitHub Issues: https://github.com/your-username/vipauto-crm/issues
- Email: support@vipauto-crm.ru
- Telegram: @vipauto_support

## 📄 Лицензия

MIT License - свободное использование и модификация

---

**🎉 Спасибо, что выбрали VIPauto CRM!**

Создано с ❤️ для автосервисов, которые ценят эффективность и современный подход.